readyState返回的数值代表的含义：
一、0：请求初始化，open还没有调用；
二、1：连接已建立，open已经调用；
三、2：请求已经接受，接收到头信息；
四、3：请求处理中，接受到响应主体；
五、4：请求已完成，且响应已就绪，也就是响已经完成。

readyState.onreadystatechange = function() {
  if(request.readyState===4 && rquest.status===200){
     //做一些事情request.responseText}
     }
